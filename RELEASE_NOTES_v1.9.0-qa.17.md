# Realm Guard / Torchbearer v1.9.0-qa.17 — Character Parchment Background

Visual-only character sheet update.

- Adds the selected parchment artwork as the Character sheet background.
- Background scales with the current Character window dimensions.
- No tiling.
- Character sheet layout, data, rules and M8 Social Network work are unchanged.
- Skills / Inventory content is untouched; this patch only changes the shared Character Actor window background.

QA:
- resize the Character window wide / narrow / tall / short
- verify the parchment always fills the window
- verify there is no tiling or exposed dark base background
- verify sheet controls remain readable and functional
- verify NPC / Item / Conflict windows are unchanged
